apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: ingress-apps
  namespace: argocd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: ssh://git@github.com/alemuro/argocd-homelab.git
        revision: HEAD
        files:
          - path: "applications/ingress/apps/*.yaml"
  template:
    metadata:
      name: "ingress-{{.name}}"
    spec:
      project: ingress
      source:
        path: helm/homelab-application
        repoURL: ssh://git@github.com/alemuro/argocd-homelab.git
        targetRevision: HEAD
        helm:
          values: |
            {{- toYaml . | nindent 12 }}
      destination:
        name: homelab
        namespace: ingress
      syncPolicy:
        automated:
          prune: false
          selfHeal: true
        syncOptions:
          - ApplyOutOfSyncOnly=true
